import { ComingSoon } from '@/components/reusable'

export default function AttendeeListTab() {
  return (
    <ComingSoon
      title='Attendee List'
      description='The full attendee list will land once an endpoint exposes individual attendees. Analytics currently returns attendee totals only.'
      showBackButton={false}
    />
  )
}
